import { motion } from "framer-motion";
import { pageVariants, staggerContainerVariants, staggerItemVariants } from "../utils/animations";
import "../Page-styling/Skills.css";

export default function Skills() {
return(
    <></>
)
}